import AgentChat from "@/components/agent/AgentChat";

export const metadata = {
  title: "Checkpoint — FL-07",
  description:
    "FlyRank FL-07: a submission-coach agent that turns an assignment brief into a saved checklist and checks work against it.",
};

export default function AgentPage() {
  return <AgentChat />;
}
